# frozen_string_literal: true

RSpec.describe SimpleSiteCrawler do
  it 'has a version number' do
    expect(SimpleSiteCrawler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
